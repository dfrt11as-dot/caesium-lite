name: caesium_lite
description: Simple image compressor for web and mobile — like Caesium, but in your browser.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^0.9.0
  flutter_image_compress: ^2.2.0
  universal_html: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
    dependency: direct